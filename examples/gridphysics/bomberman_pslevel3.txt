function mutate(){

	if random number < probability of mutation {
	
		randomly select chunk to replace
		
		if selected chunk is a border {
			randomly replace with chunk of same border type 
		} else {
			randomly replace with other pattern
		}
		
	} else {
		swap two non-border chunks
	}
	
	fix level goals()
}

